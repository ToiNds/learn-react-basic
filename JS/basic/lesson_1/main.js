var fullName = "Nguyen Dinh Toi";
var age = 20;

alert("Xin chao: " + fullName + "\nTuoi: " + age);

/**
 * Giới thiệu 1 số hàm built-in
 * 1. Alert
 * 2. Confirm
 * 3. Console
 * 4.Prompt
 * 5. setTimeout --> Chạy đoạn code sau 1 khoảng thời gian(Chỉ chạy 1 lần).
 * 6. setInterval --> Chạy đoạn code sau 1 khoảng thời gian(Chạy liên tục).
 */

var age = 35;
var nextAge = ++age;
console.log(nextAge);
